{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Stores profile information for users interacting with the AI coaching app, complementing an external authentication system by holding non-sensitive user data.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "The user's first name."
        },
        "lastName": {
          "type": "string",
          "description": "The user's last name."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the user profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "firstName",
        "createdAt"
      ]
    },
    "CoachingSession": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CoachingSession",
      "type": "object",
      "description": "Represents a single coaching session interaction between a user and the AI coach.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the CoachingSession entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the UserProfile who participated in this session. (Relationship: UserProfile 1:N CoachingSession)"
        },
        "startTime": {
          "type": "string",
          "description": "The exact timestamp when the coaching session began.",
          "format": "date-time"
        },
        "endTime": {
          "type": "string",
          "description": "The exact timestamp when the coaching session ended.",
          "format": "date-time"
        },
        "durationMinutes": {
          "type": "number",
          "description": "The total duration of the session in minutes."
        },
        "status": {
          "type": "string",
          "description": "The current or final status of the coaching session (e.g., 'started', 'completed', 'cancelled', 'failed')."
        },
        "summary": {
          "type": "string",
          "description": "A brief, AI-generated summary or outcome of the coaching session."
        }
      },
      "required": [
        "id",
        "userId",
        "startTime",
        "status"
      ]
    },
    "SessionMessage": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SessionMessage",
      "type": "object",
      "description": "Stores individual messages or turns exchanged during a CoachingSession, capturing both user input and AI responses.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the SessionMessage entity."
        },
        "sessionId": {
          "type": "string",
          "description": "Reference to the CoachingSession this message belongs to. (Relationship: CoachingSession 1:N SessionMessage)"
        },
        "sender": {
          "type": "string",
          "description": "Identifies the source of the message (e.g., 'user', 'ai')."
        },
        "contentType": {
          "type": "string",
          "description": "The type of content in the message (e.g., 'text', 'audio')."
        },
        "textContent": {
          "type": "string",
          "description": "The textual content of the message, if applicable."
        },
        "audioUrl": {
          "type": "string",
          "description": "URL to the recorded audio content of the message, if applicable.",
          "format": "uri"
        },
        "timestamp": {
          "type": "string",
          "description": "The exact timestamp when the message was sent or recorded.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "sessionId",
        "sender",
        "contentType",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores individual user profiles. Access is strictly restricted to the user whose `uid` matches the `{userId}` path parameter.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user, matching `request.auth.uid`."
            }
          ]
        }
      },
      {
        "path": "users/{userId}/coachingSessions/{sessionId}",
        "definition": {
          "entityName": "CoachingSession",
          "schema": {
            "$ref": "#/backend/entities/CoachingSession"
          },
          "description": "Stores individual coaching sessions for a specific user. Includes the `userId` field for authorization independence, allowing direct ownership checks. Access is restricted to the user whose `uid` matches the `{userId}` path parameter.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user who owns this session, matching `request.auth.uid`."
            },
            {
              "name": "sessionId",
              "description": "The unique ID of the coaching session."
            }
          ]
        }
      },
      {
        "path": "users/{userId}/coachingSessions/{sessionId}/sessionMessages/{messageId}",
        "definition": {
          "entityName": "SessionMessage",
          "schema": {
            "$ref": "#/backend/entities/SessionMessage"
          },
          "description": "Stores messages exchanged within a specific coaching session. Access is restricted to the user whose `uid` matches the denormalized `userId`.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user who owns the parent session, matching `request.auth.uid`."
            },
            {
              "name": "sessionId",
              "description": "The unique ID of the parent coaching session."
            },
            {
              "name": "messageId",
              "description": "The unique ID of the session message."
            }
          ]
        }
      }
    ],
    "reasoning": "Standard user-centric Firestore hierarchy."
  }
}
